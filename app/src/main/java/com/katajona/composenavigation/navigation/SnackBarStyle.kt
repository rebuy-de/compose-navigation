package com.katajona.composenavigation.navigation

import androidx.compose.ui.graphics.Color

enum class SnackBarStyle(val color: Color) {
    NEUTRAL(Color.Gray),
    SUCCESS(Color.Green),
    ERROR(Color.Red),
}
